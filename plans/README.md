# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Original finding | Priority | Effort | Depends on | Status |
|------|-------|------------------|----------|--------|------------|--------|
| 001 | Harden offline queue enqueue against silent failures | 4 (CORRECTNESS-04) | P1 | S | — | TODO |
| 002 | Harden offline queue merge fallback | 10 (CORRECTNESS-05) | P1 | S | 001 | TODO |
| 003 | Delete offline queue rows only after confirmed emission | 2 (CORRECTNESS-01) | P1 | S | 001, 002 | TODO |
| 004 | Make `sync_confirmed` delete only the rows it acknowledges | 3 (CORRECTNESS-02) | P1 | S/M | 003 | TODO |
| 005 | Preserve local snapshot on Yjs apply failure | 9 (CORRECTNESS-03) | P1 | S | — | DONE (code), test-run INCONCLUSIVE |
| 006 | Batch Y.Doc snapshot reads from SQLite | 18 (PERF-01) | P2 | M | — | TODO |
| 007 | Move Spotlight indexing off the main thread | 19 (PERF-02) | P2 | M | — | TODO |
| 008 | Sync `Package.swift` with the real Xcode target graph | 21 (DEPS-03) | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 and 003 touch the same `drainOfflineQueue` flow; run them in order.
- 004 builds on 003: once emission success/failure is known, the set of rows included in a request can be tracked and cleaned up precisely.
- 005 is independent of the queue plans but sits on the same sync path; run it any time after 001.
- 006 only changes the store layer and can run in parallel with sync fixes, but it touches `YjsSyncService` callers.
- 007 and 008 are fully independent.

## Findings considered and rejected

See the audit report for the full list. The plans above intentionally do **not** include test-infrastructure plans (findings 12, 13, 14) because the user selected a narrower set. Each sync plan therefore contains its own regression tests, but a dedicated `YjsSyncService` test harness (finding 13) remains the best long-term foundation.
