# Layout: <название фичи>

**Spec**: [spec.md](./spec.md)  
**Figma**: `<file-key>` · frames: `<node-id>` — <описание>

> Черновик для **ревью человеком** перед реализацией view. После согласования — источник истины для приёмки и `layout-audit.json`.

---

## Canvas

| Параметр | Значение |
|----------|----------|
| Размер / family | например `systemSmall` 169×169 |
| Padding | 16 pt |
| Content area | W×H pt (вычислить) |
| Grid gap | 12 pt |

---

## Токены

Один файл: `<Path/to/*Layout.swift>` (или CSS tokens на web).

| Token | Значение | Figma variable |
|-------|----------|----------------|
| `exampleToken` | 16 pt | … |

---

## State: `<имя>` (Figma `<node-id>`)

### Размеры блоков

| Элемент | W×H | Примечание |
|---------|-----|------------|
| … | … | … |

### Дерево (DOM)

```text
Root (content W×H)
├─ <child A> (…)
│   └─ …
└─ <child B> overlay top-trailing
```

**Критично:** явно указать, какие блоки **не** делят ширину с соседями (типичный баг: текст в колонке 89pt вместо full 137pt).

### SwiftUI / platform notes

- …
- Запреты: …

---

## Примитивы (реализовать до экранов)

| Примитив | Файл | Ответственность |
|----------|------|-----------------|
| … | … | … |

---

## Матрица приёмки

| State | Light | Dark | Mono / accessory | Edge case data |
|-------|-------|------|------------------|----------------|
| empty | ☐ | ☐ | ☐ | — |
| … | ☐ | ☐ | ☐ | длинное имя, exceeded, … |

---

## Falsifiable claims

1. **Claim:** … — измерение: …
2. …

---

## Stub data (preview / seed)

| Сценарий | Строки / таймеры | Где |
|----------|------------------|-----|
| worst-case wrap | «… длинное название» | `#Preview`, `seed-*.py` |

---

## Changelog

| Дата | Изменение |
|------|-----------|
| YYYY-MM-DD | Черновик |
