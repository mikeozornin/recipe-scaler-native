# Code Review: master (assistant user-message attachment chips)

## Summary

Uncommitted change adds web-parity recipe-attachment chips on sent user bubbles in the assistant sheet (chips under text, or chips-only when text is empty / equals a single `recipeId`). Resolver + chips-only gate + copy-text path match `assistant-message-list.tsx` for the happy path; tests cover the new gates.

**Reviewed:** Business Logic, Architecture, Performance, Standards.  
**Skipped:** Security — no authn/authz, secrets, or new untrusted input surfaces.

**Findings status (2026-08-15):** all High and Medium items fixed.

Risk after fixes: **low**.

## Findings (sorted by priority)

### High — fixed

1. **[Business Logic]** Empty collection titles → **Fixed.** `AssistantAttachableRecipeLookups.build` maps empty/whitespace names to `recipes.no-title`; resolver treats empty strings as missing; empty colors omitted from map.

2. **[Performance]** Lookups rebuilt per bubble → **Fixed.** Built once in `messageList` and passed down.

### Medium — fixed

3. **[Architecture]** Shared chip kit under Composer → **Fixed.** Extracted to `AssistantAttachmentChip.swift` (split / leading / label / chrome). Composer keeps removable wrapper; Footer keeps read-only `AssistantAttachmentChipDisplayView`.

## Areas with no findings

- **Standards** — i18n, AccessibilityIdentifiers, AppTypography, XCTest style all compliant.

## Recommendation

**Approved.**
